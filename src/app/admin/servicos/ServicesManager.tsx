"use client";
import { useEffect,useState } from "react";
import { Plus,Trash2 } from "lucide-react";
type Service={id:string;name:string;description?:string;durationMin:number;priceCents:number;active:boolean};
export default function ServicesManager(){const [items,setItems]=useState<Service[]>([]);const [loading,setLoading]=useState(true);const [msg,setMsg]=useState("");async function load(){setLoading(true);const r=await fetch('/api/admin/services');if(r.ok)setItems(await r.json());setLoading(false)}useEffect(()=>{load()},[]);async function create(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/admin/services',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:f.get('name'),description:f.get('description'),durationMin:Number(f.get('durationMin')),price:Number(f.get('price')),active:true})});if(r.ok){e.currentTarget.reset();setMsg('Serviço criado com sucesso.');load()}else setMsg('Não foi possível criar o serviço.')}async function remove(id:string){if(!confirm('Excluir este serviço?'))return;const r=await fetch('/api/admin/services/'+id,{method:'DELETE'});if(r.ok){setMsg('Serviço excluído.');load()}}return <><div className="card"><h3>Novo serviço</h3><form onSubmit={create}><div className="field"><label>Nome</label><input name="name" required/></div><div className="field"><label>Descrição</label><input name="description"/></div><div className="grid" style={{gridTemplateColumns:'1fr 1fr',marginTop:0}}><div className="field"><label>Duração (minutos)</label><input name="durationMin" type="number" min="15" defaultValue="60" required/></div><div className="field"><label>Valor (R$)</label><input name="price" type="number" min="0" step="0.01" required/></div></div><button className="btn btn-primary"><Plus size={17}/> Adicionar serviço</button>{msg&&<p>{msg}</p>}</form></div><div style={{height:20}}/>{loading?<p>Carregando...</p>:items.length===0?<div className="card"><p>Nenhum serviço cadastrado ainda. Use o formulário acima para adicionar os serviços da Clicia.</p></div>:<div className="grid">{items.map(s=><div className="card" key={s.id}><h3>{s.name}</h3><p>{s.description||'Sem descrição'}</p><div className="price">R$ {(s.priceCents/100).toFixed(2).replace('.',',')} • {s.durationMin} min</div><p>{s.active?'Ativo':'Inativo'}</p><button className="btn btn-soft" onClick={()=>remove(s.id)}><Trash2 size={16}/> Excluir</button></div>)}</div>}</>}
