import {NextResponse} from "next/server";import {adminDb} from "@/lib/firebase-admin";import {buildMonthlyReport} from "@/lib/monthly-report";
export async function GET(req:Request){if(req.headers.get("authorization")!==`Bearer ${process.env.CRON_SECRET}`)return NextResponse.json({error:"Não autorizado."},{status:401});const now=new Date(),sp=new Date(now.toLocaleString("en-US",{timeZone:"America/Sao_Paulo"})),previous=new Date(sp.getFullYear(),sp.getMonth()-1,1),month=`${previous.getFullYear()}-${String(previous.getMonth()+1).padStart(2,"0")}`,report=await buildMonthlyReport(month);await adminDb().collection("monthlyReports").doc(month).set(report);return NextResponse.json({ok:true,month})}
