import {NextResponse} from "next/server";import {adminDb} from "@/lib/firebase-admin";import {getAdminSession} from "@/lib/auth";
export async function GET(){if(!await getAdminSession())return NextResponse.json({error:"Não autorizado."},{status:401});const snap=await adminDb().collection("appointments").orderBy("startsAt","asc").limit(500).get();return NextResponse.json(snap.docs.map(d=>{const v=d.data();return{id:d.id,clientName:v.clientName||"Cliente",phone:v.phone||"",serviceName:v.serviceName||"Serviço",startsAt:v.startsAt?.toDate?.().toISOString()||"",endsAt:v.endsAt?.toDate?.().toISOString()||"",priceCents:Number(v.priceCents||0),status:v.status||"PENDING",paymentMethod:v.paymentMethod||null,notes:v.notes||null}}))}
