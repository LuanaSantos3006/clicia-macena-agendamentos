import {NextResponse} from "next/server";import {getAdminSession} from "@/lib/auth";import {buildMonthlyReport} from "@/lib/monthly-report";
export async function GET(req:Request){if(!await getAdminSession())return NextResponse.json({error:"Não autorizado."},{status:401});const month=new URL(req.url).searchParams.get("month")||"";if(!/^\d{4}-(0[1-9]|1[0-2])$/.test(month))return NextResponse.json({error:"Mês inválido."},{status:400});try{return NextResponse.json(await buildMonthlyReport(month))}catch{return NextResponse.json({error:"Não foi possível gerar o relatório."},{status:500})}}
