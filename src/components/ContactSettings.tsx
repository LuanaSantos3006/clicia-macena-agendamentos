"use client";
import {FormEvent,useEffect,useState} from "react";import {CheckCircle2,Instagram,Loader2,MessageCircle,Save} from "lucide-react";
export default function ContactSettings(){const[instagram,setInstagram]=useState(""),[whatsapp,setWhatsapp]=useState(""),[saving,setSaving]=useState(false),[message,setMessage]=useState("");useEffect(()=>{fetch("/api/admin/contact",{cache:"no-store"}).then(r=>r.json()).then(j=>{setInstagram(j.instagram||"");setWhatsapp(j.whatsapp||"")})},[]);async function submit(e:FormEvent){e.preventDefault();setSaving(true);setMessage("");const r=await fetch("/api/admin/contact",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify({instagram,whatsapp})});const j=await r.json().catch(()=>({}));setMessage(r.ok?"Instagram e WhatsApp atualizados.":j.error||"Não foi possível salvar.");setSaving(false)}return <section className="card"><h3>Instagram e WhatsApp</h3><p>Esses contatos aparecem no canto superior direito da página das clientes.</p><form onSubmit={submit}><div className="field"><label><Instagram size={16}/> Instagram</label><input value={instagram} onChange={e=>setInstagram(e.target.value)} placeholder="@cliciamacena ou link do perfil"/></div><div className="field"><label><MessageCircle size={16}/> WhatsApp</label><input value={whatsapp} onChange={e=>setWhatsapp(e.target.value)} inputMode="tel" placeholder="(11) 99999-9999"/></div><button className="btn btn-primary" disabled={saving}>{saving?<><Loader2 className="spin" size={17}/> Salvando...</>:<><Save size={17}/> Salvar contatos</>}</button>{message?<div className="notice" role="status"><CheckCircle2 size={17}/>{message}</div>:null}</form></section>}
