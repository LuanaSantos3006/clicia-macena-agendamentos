"use client";
import {useEffect,useMemo,useState} from "react";import {CalendarDays,Check,CheckCircle2,Loader2,Search,Trash2,X} from "lucide-react";
type PaymentMethod="CASH"|"PIX"|"CARD";type Appointment={id:string;clientName:string;phone:string;serviceName:string;startsAt:string;endsAt:string;priceCents:number;status:string;paymentMethod?:PaymentMethod|null;notes?:string|null};const labels:Record<string,string>={PENDING:"Pendente",CONFIRMED:"Confirmado",COMPLETED:"Concluído",CANCELLED:"Cancelado",CANCELED:"Cancelado"};const money=(value:number)=>new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(value/100);
export default function AppointmentsManager({agenda=false}:{agenda?:boolean}){const[items,setItems]=useState<Appointment[]>([]),[loading,setLoading]=useState(true),[search,setSearch]=useState(""),[filter,setFilter]=useState("ALL"),[message,setMessage]=useState(""),[payments,setPayments]=useState<Record<string,PaymentMethod|"">>({});async function load(){setLoading(true);const r=await fetch("/api/admin/appointments",{cache:"no-store"});if(r.ok){const data:Appointment[]=await r.json();setItems(data);setPayments(Object.fromEntries(data.map(i=>[i.id,i.paymentMethod||""])))}else setMessage("Não foi possível carregar os agendamentos.");setLoading(false)}useEffect(()=>{void load()},[]);const visible=useMemo(()=>items.filter(i=>{const term=search.toLowerCase(),matches=!term||i.clientName.toLowerCase().includes(term)||i.phone.includes(term)||i.serviceName.toLowerCase().includes(term),future=new Date(i.startsAt).getTime()>=Date.now()-86400000;return matches&&(filter==="ALL"||i.status===filter)&&(!agenda||future)}),[items,search,filter,agenda]);async function update(id:string,status?:string,paymentMethod?:PaymentMethod){const r=await fetch(`/api/admin/appointments/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({...status?{status}:{},...paymentMethod?{paymentMethod}:{}})});setMessage(r.ok?"Agendamento atualizado.":"Não foi possível atualizar.");if(r.ok)await load()}async function complete(id:string){const method=payments[id];if(!method){setMessage("Selecione a forma de pagamento antes de concluir.");return}await update(id,"COMPLETED",method)}async function remove(id:string){if(!confirm("Excluir este agendamento definitivamente?"))return;const r=await fetch(`/api/admin/appointments/${id}`,{method:"DELETE"});setMessage(r.ok?"Agendamento excluído.":"Não foi possível excluir.");if(r.ok)await load()}if(loading)return <div className="loading-row"><Loader2 className="spin"/> Carregando agenda...</div>;return <><div className="admin-toolbar"><label className="admin-search"><Search size={17}/><input aria-label="Pesquisar" placeholder="Buscar cliente, telefone ou serviço" value={search} onChange={e=>setSearch(e.target.value)}/></label><select aria-label="Filtrar status" value={filter} onChange={e=>setFilter(e.target.value)}><option value="ALL">Todos os status</option><option value="PENDING">Pendentes</option><option value="CONFIRMED">Confirmados</option><option value="COMPLETED">Concluídos</option><option value="CANCELLED">Cancelados</option></select></div>{message?<div className="notice" role="status">{message}</div>:null}{visible.length===0?<div className="empty-state"><CalendarDays/><h3>Nenhum agendamento</h3><p>A agenda está livre e pronta para receber novas clientes.</p></div>:<div className="appointment-list">{visible.map(item=>{const date=new Date(item.startsAt),active=!["CANCELLED","CANCELED"].includes(item.status);return <article className="appointment-card" key={item.id}><div className="appointment-date"><strong>{date.toLocaleDateString("pt-BR",{day:"2-digit",month:"short",timeZone:"America/Sao_Paulo"})}</strong><span>{date.toLocaleTimeString("pt-BR",{hour:"2-digit",minute:"2-digit",timeZone:"America/Sao_Paulo"})}</span></div><div className="appointment-info"><div><span className={`status-pill status-${item.status.toLowerCase()}`}>{labels[item.status]||item.status}</span><h3>{item.clientName}</h3><p>{item.serviceName} • {money(item.priceCents)}</p><a href={`https://wa.me/55${item.phone}`} target="_blank" rel="noreferrer">{item.phone}</a>{item.notes?<small>Observação: {item.notes}</small>:null}</div><div className="appointment-controls">{active?<label className="payment-select"><span>Forma de pagamento</span><select value={payments[item.id]||""} onChange={e=>setPayments(v=>({...v,[item.id]:e.target.value as PaymentMethod}))}><option value="">Selecione</option><option value="CASH">Dinheiro</option><option value="PIX">Pix</option><option value="CARD">Cartão</option></select></label>:null}<div className="appointment-actions">{item.status==="PENDING"?<button className="btn btn-primary" onClick={()=>update(item.id,"CONFIRMED")}><Check size={16}/> Confirmar</button>:null}{active&&item.status!=="COMPLETED"?<button className="btn btn-soft" onClick={()=>complete(item.id)}><CheckCircle2 size={16}/> Concluir</button>:null}{item.status==="COMPLETED"&&payments[item.id]!==item.paymentMethod?<button className="btn btn-soft" onClick={()=>update(item.id,undefined,payments[item.id]||undefined)}>Salvar pagamento</button>:null}{active&&item.status!=="COMPLETED"?<button className="btn btn-soft" onClick={()=>update(item.id,"CANCELLED")}><X size={16}/> Cancelar</button>:null}<button className="icon-button danger" aria-label="Excluir" onClick={()=>remove(item.id)}><Trash2 size={17}/></button></div></div></div></article>})}</div>}</>}
